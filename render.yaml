services:
  - type: web
    name: telegram-jewish-joke-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app"
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
